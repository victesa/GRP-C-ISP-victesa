import { decodeHex, encodeHex } from "@jsr/std__encoding/hex";
/**
 * Helper to work with binary data passed into the WASM
 */ export class Bytes {
  static array(array) {
    return new Bytes({
      t: 'array',
      array
    });
  }
  static hex(hex) {
    return new Bytes({
      t: 'hex',
      hex
    });
  }
  #repr;
  constructor(data){
    this.#repr = data;
  }
  get repr() {
    return this.#repr;
  }
  hex() {
    if (this.#repr.t == 'hex') return this.#repr.hex;
    return encodeHex(this.#repr.array);
  }
  array() {
    if (this.#repr.t === 'array') return this.#repr.array;
    return decodeHex(this.#repr.hex);
  }
}
//# sourceMappingURL=util.js.map