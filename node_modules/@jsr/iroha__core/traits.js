/**
 * Ordering "trait". Tells how to compare values of the same type with each other.
 */ function ordCompareString(a, b) {
  return a.localeCompare(b);
}
function ordCompareNum(a, b) {
  return Number(a - b);
}
/**
 * Implementation of the compare function for types that are either {@link Ord}
 * or for which ordering is "known" (i.e. {@link OrdKnown}).
 *
 * See {@link Ord.compare}.
 */ export function ordCompare(a, b) {
  if (typeof a === 'string') return ordCompareString(a, b);
  if (typeof a === 'bigint' || typeof a === 'number') return ordCompareNum(a, b);
  return a.compare(b);
}
//# sourceMappingURL=traits.js.map