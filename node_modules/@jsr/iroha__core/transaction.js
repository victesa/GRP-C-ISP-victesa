import * as dm from "./data-model/mod.js";
/**
 * Build {@link dm.TransactionPayload}.
 *
 * @param executable
 * @param params
 * @returns
 */ export function buildTransactionPayload(executable, params) {
  const payload = {
    chain: params.chain,
    authority: params.authority,
    instructions: executable,
    creationTime: params.creationTime ?? dm.Timestamp.fromDate(new Date()),
    timeToLive: params.timeToLive ?? new dm.NonZero(dm.Duration.fromMillis(100_000)),
    nonce: params.nonce ?? null,
    metadata: params.metadata ?? []
  };
  return payload;
}
//# sourceMappingURL=transaction.js.map